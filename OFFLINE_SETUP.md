# Offline setup

Replace/upload the entire project to GitHub, preserving folders. Deploy `app.py` in Streamlit. Select **Offline Generator**. No API key is required for offline mode.

The offline engine is deterministic and rule-based. Update `config/teamcenter_config.json` with the exact values from your environment.
